name: Lint delivery evidence
on:
  push:
    branches: [test/lint-delivery-20260923]
permissions:
  contents: read
  actions: read
  pull-requests: read
  checks: read
  statuses: read
jobs:
  collect:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          ref: 79c24a8caec35af234c3419823c266ed346f6aca
          fetch-depth: 0
          persist-credentials: false
      - uses: ./.github/actions/setup-project
      - name: Require the exact PR CI run to finish successfully
        env:
          GH_TOKEN: ${{ github.token }}
        run: timeout 180 gh run watch 35773100966 --repo apaapapapapa/FantasySimulation --exit-status --interval 10
      - name: Collect authoritative PR and completed review evidence
        env:
          GH_TOKEN: ${{ github.token }}
        run: vp exec node scripts/harness.ts github-snapshot apaapapapapa/FantasySimulation 49 .generated/harness/pr49-final-before-merge
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        if: always()
        with:
          name: lint-pr-final-delivery
          path: .generated/harness/pr49-final-before-merge/
          include-hidden-files: true
          retention-days: 7
          if-no-files-found: error
