# 将来の分析用領域

Pythonが必要になった段階で、相性分析・能力の組み合わせ探索・統計処理を追加します。
現在のアプリの起動やテストにPythonは不要です。

分析データにはSQLiteの`battles.record_json`を使えます。
キャラクターの当時の定義、ルール版、勝敗、行動ログが含まれます。
読み取り専用の接続またはエクスポートを使い、分析側でアプリのDBを直接更新しない構成を想定しています。

ランキングを比較する際は、ルール版・対戦条件・試行回数を揃えてください。
乱数導入後はシードも保存し、結果を再現できるようにします。
